//import indexedDB from "./indexedDB.js";
//import rxdbApp from "./rxdbApp";

export const connectDatabase = async () => {
  global.log("**************** localDatabase:: connectDatabase:: ")
  //return await new rxdbApp().connect();
}

export const loadAppData = async (force) => {
  //const db = await connectDatabase();
  //global.log("**************** localDatabase:: load AppData:: ", db)

}

export const saveAppData = async (force) => {
}

export const deleteAppData = async (force) => {
}
